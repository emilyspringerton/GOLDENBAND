# v0: format + tooling only, no motion corpus checked in yet -- but real,
# small, deliberately-checked-in assets DO now exist (revisited 2026-08-26
# per this file's own note below): assets/anim/rotation_ease.gband(.json),
# a baked scalar ease curve REDGARDEN's Abraham fireball mod consumes.
# HQ-SPEC-SIM-100 §3 wants the motion library committed and
# Apples-audited, not gitignored -- so real assets are explicitly
# un-ignored below rather than force-added one at a time.
*.gband
*.gband.json
!/assets/anim/rotation_ease.gband
!/assets/anim/rotation_ease.gband.json
*.o
/tools/gbtool/gbtool
